module.exports = {
  currentExchangeRate:
    "function currentExchangeRate() view returns (uint256 vaultToken, uint256 baseToken)",
  totalBalance:
    "function totalBalance() public view returns (uint256)"
};

