{
  "deployer": [
    "event RTokenCreated(address indexed main, address indexed rToken, address stRSR, address indexed owner, string version)"
  ],
  "main": [
    "function backingManager() view returns (address)",
    "function basketHandler() view returns (address)",
    "function stRSR() view returns (address)"
  ],
  "basketHandler": [
    "function quote(uint192, uint8) view returns (address[], uint256[])"
  ]
}
