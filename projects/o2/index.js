const { customScriptTvlExports } = require('../helper/utils');

module.exports = customScriptTvlExports({
  fuel: 'o2',
})

module.exports.methodology = 'The TVL is defined as the total sum of assets across all user trading accounts'
