{
  "depositId": "uint256:depositId",
  "getDepositDetails": "function getDepositDetails(uint256 _id) view returns (
            address tokenAddress,
            address withdrawalAddress,
            uint256 tokenAmount,
            uint256 unlockTime,
            string memory lockName,
            string memory lockDescription,
            bool withdrawn
        )"
}
