# Multipli Yield child

Formula: `remaining V1 + V2 totalAssets`.

V1 remains on Ethereum and BSC and is intentionally preserved until migration.
The adapter validates and allowlists the V1 response. V2 is read onchain. rwaUSDi
and xToken share supply are excluded from this child.
